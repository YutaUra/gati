# Changed Files Filter

## What

Add a toggle (`g` key) to the file tree that switches between showing all files and showing only files with git changes (plus their ancestor directories).

## Why

When reviewing code in a large repository, users need to focus on what changed. Filtering to only changed files eliminates noise and speeds up review.

## Capabilities

- **Modified capability: file-tree** — Filter toggle, dynamic title, filtered tree rendering
